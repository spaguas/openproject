<%= render "homescreen/page_header", selected: :team_allocation %>

<%= render(
      Overviews::TeamAllocationDashboardComponent.new(
        dashboard: @team_allocation_dashboard,
        form_url: global_team_allocation_path,
        context: :global
      )
    ) %>
